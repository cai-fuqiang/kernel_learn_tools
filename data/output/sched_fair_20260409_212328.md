# Linux 内核邮件讨论报告
## 主题: sched/fair
## 线程数: 3
## 生成时间: 2026年04月09日 21:23

---

## 总览统计

| 指标 | 数值 |
|------|------|
| 线程数 | 3 |
| 总邮件数 | 3 |
| 参与者数 | 2 |

### 主要参与者

| 参与者 | 参与线程数 |
|--------|------------|
| Shrikanth Hegde <> | 2 |
| Christian Loehle <> | 1 |

---

## 线程 1: Re: [PATCH] sched/fair: Fix stale comment referencing update_cfs_shares()

**参与者**: Christian Loehle <>

**时间范围**: Wed, 8 Apr 2026 09:59:41 +0100 至 Wed, 8 Apr 2026 09:59:41 +0100

### 原邮件

**作者**: Christian Loehle <>
**日期**: Wed, 8 Apr 2026 09:59:41 +0100
**主题**: Re: [PATCH] sched/fair: Fix stale comment referencing update_cfs_shares()

**主题翻译**: 回复：[PATCH] sched/fair：修复引用 update_cfs_shares() 的陈旧评论

**原文**:
```
On 4/2/26 04:13, Zhan Xusheng wrote:
> update_cfs_shares() has been renamed to update_cfs_group(),
> but some comments still refer to the old function name.
> 
> Update these comments to reflect the current code and avoid confusion.
> 
> Signed-off-by: Zhan Xusheng <zhanxusheng@xiaomi.com>
> ---
>  kernel/sched/fair.c | 4 ++--
>  1 file changed, 2 insertions(+), 2 deletions(-)
> 
> diff --git a/kernel/sched/fair.c b/kernel/sched/fair.c
> index bf948db905ed..172194919c33 100644
> --- a/kernel/sched/fair.c
> +++ b/kernel/sched/fair.c
> @@ -4155,7 +4155,7 @@ static inline bool cfs_rq_is_decayed(struct cfs_rq *cfs_rq)
>   * differential update where we store the last value we propagated. This in
>   * turn allows skipping updates if the differential is 'small'.
>   *
> - * Updating tg's load_avg is necessary before update_cfs_share().
> + * Updating tg's load_avg is necessary before update_cfs_group().
>   */
>  static inline void update_tg_load_avg(struct cfs_rq *cfs_rq)
>  {
> @@ -4615,7 +4615,7 @@ static void migrate_se_pelt_lag(struct sched_entity *se) {}
>   * The cfs_rq avg is the direct sum of all its entities (blocked and runnable)
>   * avg. The immediate corollary is that all (fair) tasks must be attached.
>   *
> - * cfs_rq->avg is used for task_h_load() and update_cfs_share() for example.
> + * cfs_rq->avg is used for task_h_load() and update_cfs_group() for example.
>   *
>   * Return: true if the load decayed or we removed load.
>   *

Should mention
1ea6c46a23f1 ("sched/fair: Propagate an effective runnable_load_avg")
apart from that:

Reviewed-by: Christian Loehle <christian.loehle@arm.com>
```

**翻译**:
```
26年4月2日 04:13，詹旭升写道：
> update_cfs_shares() 已重命名为 update_cfs_group(),
> 但一些注释仍然引用旧的函数名称。
> 
> 更新这些注释以反映当前代码并避免混淆。
> 
> 签字人：詹旭升 <zhanxusheng@xiaomi.com>
> ---
> 内核/sched/fair.c | 4++--
> 1 个文件已更改、2 个插入(+)、2 个删除(-)
> 
> diff --git a/kernel/sched/fair.c b/kernel/sched/fair.c
> 索引 BF948db905ed..172194919c33 100644
> --- a/kernel/sched/fair.c
> +++ b/kernel/sched/fair.c
> @@ -4155,7 +4155,7 @@ 静态内联 bool cfs_rq_is_decayed(struct cfs_rq *cfs_rq)
> * 差异更新，我们存储我们传播的最后一个值。这在
> * 如果差异“小”，turn 允许跳过更新。
> *
> - * 在 update_cfs_share() 之前需要更新 tg 的 load_avg。
> + * 在 update_cfs_group() 之前需要更新 tg 的 load_avg。
> */
> 静态内联 void update_tg_load_avg(struct cfs_rq *cfs_rq)
> {
> @@ -4615,7 +4615,7 @@ static void migrate_se_pelt_lag(struct sched_entity *se) {}
> * cfs_rq avg 是其所有实体（阻塞的和可运行的）的直接总和
> * 平均。直接的推论是必须附加所有（公平）任务。
> *
> - * cfs_rq->avg 用于例如task_h_load() 和update_cfs_share()。
> + * cfs_rq->avg 用于例如task_h_load() 和update_cfs_group()。
> *
> * 返回：如果负载衰减或我们移除负载，则返回 true。
> *

应该提到
1ea6c46a23f1（“sched/fair：传播有效的runnable_load_avg”）
除此之外：

审阅者：Christian Loehle <christian.loehle@arm.com>
```


---

## 线程 2: [PATCH v2 07/17] sched/fair: load balance only among preferred CPUs

**参与者**: Shrikanth Hegde <>

**时间范围**: Wed,  8 Apr 2026 00:49:40 +0530 至 Wed,  8 Apr 2026 00:49:40 +0530

### 原邮件

**作者**: Shrikanth Hegde <>
**日期**: Wed,  8 Apr 2026 00:49:40 +0530
**主题**: [PATCH v2 07/17] sched/fair: load balance only among preferred CPUs

**主题翻译**: [PATCH v2 07/17] sched/fair：仅在首选 CPU 之间进行负载平衡

**原文**:
```
Consider only preferred CPUs for load balance.

With this, load balance will end up choosing a preferred CPUs for pull.
This makes it not fight against the push task mechanism which happens
at tick. Also, this stops active balance to happen on non-preferred CPU
pulling the load.

This means there is no load balancing if the task is pinned only to
non-preferred CPUs. They will continue to run where they were previously
running before the CPUs was marked as non-preferred.

Signed-off-by: Shrikanth Hegde <sshegde@linux.ibm.com>
---
 kernel/sched/fair.c | 5 +++++
 1 file changed, 5 insertions(+)

diff --git a/kernel/sched/fair.c b/kernel/sched/fair.c
index 22010afb4c1d..e4571bd71a44 100644
--- a/kernel/sched/fair.c
+++ b/kernel/sched/fair.c
@@ -12058,6 +12058,11 @@ static int sched_balance_rq(int this_cpu, struct rq *this_rq,
 
 	cpumask_and(cpus, sched_domain_span(sd), cpu_active_mask);
 
+#ifdef CONFIG_PARAVIRT
+	/* Spread load among preferred CPUs */
+	cpumask_and(cpus, cpus, cpu_preferred_mask);
+#endif
+
 	schedstat_inc(sd->lb_count[idle]);
 
 redo:
-- 
2.47.3
```

**翻译**:
```
仅考虑用于负载平衡的首选 CPU。

这样，负载平衡最终将选择首选 CPU 进行拉取。
这使得它不会对抗发生的推送任务机制
在刻度线。此外，这会阻止非首选 CPU 上发生主动平衡
拉动负载。

这意味着如果任务仅固定到
非首选 CPU。他们将继续在原来的地方奔跑
在 CPU 被标记为非首选之前运行。

签署人：Shrikanth Hegde <sshegde@linux.ibm.com>
---
 内核/sched/fair.c | 5 +++++
 更改了 1 个文件，插入了 5 次(+)

diff --git a/kernel/sched/fair.c b/kernel/sched/fair.c
索引 22010afb4c1d..e4571bd71a44 100644
---a/kernel/sched/fair.c
+++ b/kernel/sched/fair.c
@@ -12058,6 +12058,11 @@ static int sched_balance_rq(int this_cpu, struct rq *this_rq,
 
 	cpumask_and(cpus, sched_domain_span(sd), cpu_active_mask);
 
+#ifdef CONFIG_PARAVIRT
+ /* 在首选 CPU 之间分散负载 */
+ cpumask_and(cpu, cpu, cpu_preferred_mask);
+#endif
+
 	schedstat_inc(sd->lb_count[空闲]);
 
 重做：
-- 
2.47.3
```


---

## 线程 3: [PATCH v2 06/17] sched/fair: Select preferred CPU at wakeup when possible

**参与者**: Shrikanth Hegde <>

**时间范围**: Wed,  8 Apr 2026 00:49:39 +0530 至 Wed,  8 Apr 2026 00:49:39 +0530

### 原邮件

**作者**: Shrikanth Hegde <>
**日期**: Wed,  8 Apr 2026 00:49:39 +0530
**主题**: [PATCH v2 06/17] sched/fair: Select preferred CPU at wakeup when possible

**主题翻译**: [PATCH v2 06/17] sched/fair：如果可能，在唤醒时选择首选 CPU

**原文**:
```
Update available_idle_cpu to consider preferred CPUs. This takes care of
lot of decisions at wakeup to use only preferred CPUs. There is no need to
put those explicit checks everywhere.

Only other place where prev_cpu was not preferred and could possibly return
was sched_balance_find_dst_cpu. Put the check there.

Signed-off-by: Shrikanth Hegde <sshegde@linux.ibm.com>
---
 kernel/sched/fair.c  | 3 ++-
 kernel/sched/sched.h | 3 +++
 2 files changed, 5 insertions(+), 1 deletion(-)

diff --git a/kernel/sched/fair.c b/kernel/sched/fair.c
index 86ef9ce39b61..22010afb4c1d 100644
--- a/kernel/sched/fair.c
+++ b/kernel/sched/fair.c
@@ -7698,7 +7698,8 @@ static inline int sched_balance_find_dst_cpu(struct sched_domain *sd, struct tas
 {
 	int new_cpu = cpu;
 
-	if (!cpumask_intersects(sched_domain_span(sd), p->cpus_ptr))
+	if (!cpumask_intersects(sched_domain_span(sd), p->cpus_ptr) &&
+	    cpu_preferred(prev_cpu))
 		return prev_cpu;
 
 	/*
diff --git a/kernel/sched/sched.h b/kernel/sched/sched.h
index 7271af2ca64f..4c45092b2fce 100644
--- a/kernel/sched/sched.h
+++ b/kernel/sched/sched.h
@@ -1412,6 +1412,9 @@ static inline bool available_idle_cpu(int cpu)
 	if (!idle_rq(cpu_rq(cpu)))
 		return 0;
 
+	if (!cpu_preferred(cpu))
+		return 0;
+
 	if (vcpu_is_preempted(cpu))
 		return 0;
 
-- 
2.47.3
```

**翻译**:
```
更新 available_idle_cpu 以考虑首选 CPU。这照顾
在唤醒时做出很多只使用首选 CPU 的决定。没有必要
将这些明确的检查无处不在。

只有 prev_cpu 不被首选并且可能返回的其他地方
是 sched_balance_find_dst_cpu。把支票放在那里。

签署人：Shrikanth Hegde <sshegde@linux.ibm.com>
---
 内核/sched/fair.c | 3++-
 内核/sched/sched.h | 3+++
 2 个文件更改、5 个插入(+)、1 个删除(-)

diff --git a/kernel/sched/fair.c b/kernel/sched/fair.c
索引 86ef9ce39b61..22010afb4c1d 100644
---a/kernel/sched/fair.c
+++ b/kernel/sched/fair.c
@@ -7698,7 +7698,8 @@ 静态内联 int sched_balance_find_dst_cpu(struct sched_domain *sd, struct tas
 {
 	int new_cpu = cpu;
 
- if (!cpumask_intersects(sched_domain_span(sd), p->cpus_ptr))
+ if (!cpumask_intersects(sched_domain_span(sd), p->cpus_ptr) &&
+ cpu_preferred(prev_cpu))
 		返回上一个CPU；
 
 	/*
diff --git a/kernel/sched/sched.h b/kernel/sched/sched.h
索引 7271af2ca64f..4c45092b2fce 100644
---a/kernel/sched/sched.h
+++ b/kernel/sched/sched.h
@@ -1412,6 +1412,9 @@ 静态内联 bool available_idle_cpu(int cpu)
 	if (!idle_rq(cpu_rq(cpu)))
 		返回0；
 
+ if (!cpu_preferred(cpu))
+返回0；
+
 	if (vcpu_is_preempted(cpu))
 		返回0；
 
-- 
2.47.3
```


---

